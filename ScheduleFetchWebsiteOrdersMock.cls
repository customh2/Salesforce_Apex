global class ScheduleFetchWebsiteOrdersMock implements HttpCalloutMock {
    global HTTPResponse respond(HTTPRequest req) {
        system.Debug(' -- MOCK -- Mock HTTPRequest respond() function called.');
    	// build the response body
    	String XML = '<?xml version="1.0" standalone="yes" ?>\n';
    	XML = XML + '  <Salesforce>\n';
        XML = XML + '    <Parameters>\n';
        XML = XML + '      <StartGMT>2015-10-16T18:25:00Z</StartGMT>\n';
        XML = XML + '      <StartLocal>2015-10-16T18:25:00Z</StartLocal>\n';
        XML = XML + '      <End>2015-10-16 18:32:09</End>\n';
		XML = XML + '      <MaxCount>50</MaxCount>\n';
		XML = XML + '    </Parameters>\n';
    	XML = XML + '    <Orders>\n';
    	XML = XML + '      <Order>\n';
    	XML = XML + '        <OrderNumber>1001</OrderNumber>\n';
    	XML = XML + '        <OrderDate>2019-01-01T01:00:00</OrderDate>\n';
    	XML = XML + '        <LastModified>2019-01-01T01:00:00</LastModified>\n';
    	XML = XML + '        <ShippingMethod>Fedex</ShippingMethod>\n';
    	XML = XML + '        <StatusCode>New</StatusCode>\n';
    	XML = XML + '        <Notes>\n';
		XML = XML + '        	<Note public="true" date="2015-10-16T18:26:59">checkout_secure</Note>\n';
		XML = XML + '        </Notes>\n';
    	XML = XML + '        <ShippingAddress>\n';
    	XML = XML + '          <FullName>Test1 Customer1</FullName>\n';
    	XML = XML + '          <FirstName>Test1</FirstName>\n';
    	XML = XML + '          <LastName>Customer1</LastName>\n';
    	XML = XML + '          <Company>Test Company 1</Company>\n';
    	XML = XML + '          <Street1>101 NW 1st Ave</Street1>\n';
    	XML = XML + '          <Street2>Apt 101</Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Pompano Beach</City>\n';
    	XML = XML + '          <State>FL</State>\n';
    	XML = XML + '          <PostalCode>33069</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>954-555-1212</Phone>\n';
    	XML = XML + '          <Email>TestCustomer1@somewhere.com</Email>\n';
    	XML = XML + '        </ShippingAddress>\n';
    	XML = XML + '        <BillingAddress>\n';
    	XML = XML + '          <FullName>Test1 Customer1</FullName>\n';
    	XML = XML + '          <FirstName>Test1</FirstName>\n';
    	XML = XML + '          <LastName>Customer1</LastName>\n';
    	XML = XML + '          <Company>Test Company 1</Company>\n';
    	XML = XML + '          <Street1>101 NW 1st Ave</Street1>\n';
    	XML = XML + '          <Street2>Apt 101</Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Pompano Beach</City>\n';
    	XML = XML + '          <State>FL</State>\n';
    	XML = XML + '          <PostalCode>33069</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>954-555-1212</Phone>\n';
    	XML = XML + '          <Email>TestCustomer1@somewhere.com</Email>\n';
    	XML = XML + '        </BillingAddress>\n';
    	XML = XML + '        <Payment>\n';
    	XML = XML + '          <Method>Credit Card</Method>\n';
    	XML = XML + '          <CreditCard>\n';
    	XML = XML + '            <Type>Visa</Type>\n';
    	XML = XML + '            <Owner>Test1 Customer1</Owner>\n';
    	XML = XML + '            <Number>********</Number>\n';
    	XML = XML + '            <Expires>01/2021</Expires>\n';
    	XML = XML + '          </CreditCard>\n';
    	XML = XML + '          <TransactionID>ABCDEF123456</TransactionID>\n';
    	XML = XML + '        </Payment>\n';
    	XML = XML + '        <Items>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10001</ItemID>\n';
    	XML = XML + '            <ProductID>10001</ProductID>\n';
    	XML = XML + '            <Code>TEST-SKU-1</Code>\n';
    	XML = XML + '            <Name>Test Part #1</Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>100.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10002</ItemID>\n';
    	XML = XML + '            <ProductID>10002</ProductID>\n';
    	XML = XML + '            <Code>GM212-B001P</Code>\n';
    	XML = XML + '            <Name>2000-2005 Chevy Impala Headlights Headlamps Left+Right Pair Set </Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>110.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '            <Attributes>\n';
    	XML = XML + '              <Attribute>\n';
    	XML = XML + '                <AttributeID>1</AttributeID>\n';
    	XML = XML + '                <Name>Extended Warranty</Name>\n';
    	XML = XML + '                <Value>One Year Warranty</Value>\n';
    	XML = XML + '                <Price>10.00</Price>\n';
    	XML = XML + '              </Attribute>\n';
    	XML = XML + '            </Attributes>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '        </Items>\n';
    	XML = XML + '        <Totals>\n';
    	XML = XML + '          <Total id="0" name="subtotal" class="subtotal" impact="none">220.00</Total>\n';
    	XML = XML + '          <Total id="0" name="shipping" class="shipping" impact="add">0.00</Total>\n';
    	XML = XML + '          <Total id="0" name="sales tax" class="tax" impact="add">13.20</Total>\n';
    	XML = XML + '          <Total id="0" name="coupon" class="coupon" impact="subtract">10.00</Total>\n';
    	XML = XML + '        </Totals>\n';
    	XML = XML + '      </Order>\n';
// ---- a second order to test other aspects of the fetch/parse routines -----
    	XML = XML + '      <Order>\n';
    	XML = XML + '        <OrderNumber>1002</OrderNumber>\n';
    	XML = XML + '        <OrderDate>2019-01-01T01:01:00</OrderDate>\n';
    	XML = XML + '        <LastModified>2019-01-01T01:01:00</LastModified>\n';
    	XML = XML + '        <ShippingMethod>Fedex</ShippingMethod>\n';
    	XML = XML + '        <StatusCode>New</StatusCode>\n';
    	XML = XML + '        <Notes>\n';
		XML = XML + '        	<Note public="true" date="2015-10-16T18:26:59">checkout_secure</Note>\n';
		XML = XML + '        </Notes>\n';
    	XML = XML + '        <ShippingAddress>\n';
    	XML = XML + '          <FullName>Vikkia Gill</FullName>\n';
    	XML = XML + '          <FirstName>Vikkia</FirstName>\n';
    	XML = XML + '          <LastName>Gill</LastName>\n';
    	XML = XML + '          <Company></Company>\n';
    	XML = XML + '          <Street1>446 E High St.</Street1>\n';
    	XML = XML + '          <Street2></Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Philadelphia</City>\n';
    	XML = XML + '          <State>PA</State>\n';
    	XML = XML + '          <PostalCode>19144</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>2673453367</Phone>\n';
    	XML = XML + '          <Email>voneal1105@gmail.com</Email>\n';
    	XML = XML + '        </ShippingAddress>\n';
    	XML = XML + '        <BillingAddress>\n';
    	XML = XML + '          <FullName>Vikkia Gill</FullName>\n';
    	XML = XML + '          <FirstName>Vikkia</FirstName>\n';
    	XML = XML + '          <LastName>Gill</LastName>\n';
    	XML = XML + '          <Company></Company>\n';
    	XML = XML + '          <Street1>446 E High St.</Street1>\n';
    	XML = XML + '          <Street2></Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Philadelphia</City>\n';
    	XML = XML + '          <State>PA</State>\n';
    	XML = XML + '          <PostalCode>19144</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>2673453367</Phone>\n';
    	XML = XML + '          <Email>voneal1105@gmail.com</Email>\n';
    	XML = XML + '        </BillingAddress>\n';
    	XML = XML + '        <Payment>\n';
    	XML = XML + '          <Method>Credit Card</Method>\n';
    	XML = XML + '          <CreditCard>\n';
    	XML = XML + '            <Type>Visa</Type>\n';
    	XML = XML + '            <Owner>Jay Tannenbaum</Owner>\n';
    	XML = XML + '            <Number>********</Number>\n';
    	XML = XML + '            <Expires>01/2022</Expires>\n';
    	XML = XML + '          </CreditCard>\n';
    	XML = XML + '          <TransactionID>ABCDEF34567</TransactionID>\n';
    	XML = XML + '        </Payment>\n';
    	XML = XML + '        <Items>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10002</ItemID>\n';
    	XML = XML + '            <ProductID>10002</ProductID>\n';
    	XML = XML + '            <Code>GM212-B001P</Code>\n';
    	XML = XML + '            <Name>2000-2005 Chevy Impala Headlights Headlamps Left+Right Pair Set </Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>110.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10003</ItemID>\n';
    	XML = XML + '            <ProductID>10003</ProductID>\n';
    	XML = XML + '            <Code>TEST-SKU-3</Code>\n';
    	XML = XML + '            <Name>Test Part #3</Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>120.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '            <Attributes>\n';
    	XML = XML + '              <Attribute>\n';
    	XML = XML + '                <AttributeID>1</AttributeID>\n';
    	XML = XML + '                <Name>Extended Warranty</Name>\n';
    	XML = XML + '                <Value>One Year Warranty</Value>\n';
    	XML = XML + '                <Price>10.00</Price>\n';
    	XML = XML + '              </Attribute>\n';
    	XML = XML + '            </Attributes>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '        </Items>\n';
    	XML = XML + '        <Totals>\n';
    	XML = XML + '          <Total id="0" name="subtotal" class="subtotal" impact="none">240.00</Total>\n';
    	XML = XML + '          <Total id="0" name="shipping" class="shipping" impact="add">0.00</Total>\n';
    	XML = XML + '          <Total id="0" name="sales tax" class="tax" impact="add">14.40</Total>\n';
    	XML = XML + '          <Total id="0" name="coupon" class="coupon" impact="subtract">10.00</Total>\n';
    	XML = XML + '        </Totals>\n';
    	XML = XML + '      </Order>\n';
// ------ a third order to generate errors (to test error handling sections of code ------
    	XML = XML + '      <Order>\n';
    	XML = XML + '        <OrderNumber>1003</OrderNumber>\n';
    	XML = XML + '        <OrderDate>2019-01-01T01:02:00</OrderDate>\n';
    	XML = XML + '        <LastModified>2019-01-01T02:01:00</LastModified>\n';
    	XML = XML + '        <ShippingMethod>Fedex</ShippingMethod>\n';
    	XML = XML + '        <StatusCode>New</StatusCode>\n';
    	// don't send Order Notes
    	//XML = XML + '        <Notes>\n';
		//XML = XML + '        	<Note public="true" date="2015-10-16T18:26:59">checkout_secure</Note>\n';
		//XML = XML + '        </Notes>\n';
    	XML = XML + '        <ShippingAddress>\n';
    	XML = XML + '          <FullName>Jay Tannenbaum</FullName>\n';
    	XML = XML + '          <FirstName>Jay</FirstName>\n';
    	XML = XML + '          <LastName>Tannenbaum</LastName>\n';
    	XML = XML + '          <Company>Performance Depot Inc</Company>\n';
    	XML = XML + '          <Street1>11129 Mandarin Street</Street1>\n';
    	XML = XML + '          <Street2></Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Boca Raton</City>\n';
    	XML = XML + '          <State>FL</State>\n';
    	XML = XML + '          <PostalCode>33428</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>ABCD1234</Phone>\n'; // Invalid Phone
    	XML = XML + '          <Email>dank@pdiautoparts.com</Email>\n'; // Invalid Email
    	XML = XML + '        </ShippingAddress>\n';
    	XML = XML + '        <BillingAddress>\n';
    	XML = XML + '          <FullName>Jay Tannenbaum</FullName>\n';
    	XML = XML + '          <FirstName>Jay</FirstName>\n';
    	XML = XML + '          <LastName>Tannenbaum</LastName>\n';
    	XML = XML + '          <Company>Performance Deoit Inc.</Company>\n';
    	XML = XML + '          <Street1>11129 Mandarin Street</Street1>\n';
    	XML = XML + '          <Street2></Street2>\n';
    	XML = XML + '          <Street3></Street3>\n';
    	XML = XML + '          <City>Boca Raton</City>\n';
    	XML = XML + '          <State>FL</State>\n';
    	XML = XML + '          <PostalCode>33428</PostalCode>\n';
    	XML = XML + '          <Country>US</Country>\n';
    	XML = XML + '          <Phone>ABCD1234</Phone>\n'; // Invalid Phone
    	XML = XML + '          <Email>dank@pdiautoparts.com</Email>\n'; // Invalid Email
    	XML = XML + '        </BillingAddress>\n';
    	XML = XML + '        <Payment>\n';
    	XML = XML + '          <Method>Credit Card</Method>\n';
    	XML = XML + '          <CreditCard>\n';
    	XML = XML + '            <Type>Visa</Type>\n';
    	XML = XML + '            <Owner>Jay Tannenbaum</Owner>\n';
    	XML = XML + '            <Number>********</Number>\n';
    	XML = XML + '            <Expires>01/2022</Expires>\n';
    	XML = XML + '          </CreditCard>\n';
    	XML = XML + '          <TransactionID>CDEFGH987654</TransactionID>\n';
    	XML = XML + '        </Payment>\n';
    	XML = XML + '        <Items>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10002</ItemID>\n';
    	XML = XML + '            <ProductID>10002</ProductID>\n';
    	XML = XML + '            <Code>TEST-SKU-2</Code>\n';
    	XML = XML + '            <Name>Test Part #2</Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>110.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '          <Item>\n';
    	XML = XML + '            <ItemID>10003</ItemID>\n';
    	XML = XML + '            <ProductID>10003</ProductID>\n';
    	XML = XML + '            <Code>TEST-SKU-3</Code>\n';
    	XML = XML + '            <Name>Test Part #3</Name>\n';
    	XML = XML + '            <Quantity>1</Quantity>\n';
    	XML = XML + '            <UnitPrice>120.00</UnitPrice>\n';
    	XML = XML + '            <Image></Image>\n';
    	XML = XML + '            <Weight>0</Weight>\n';
    	XML = XML + '            <Attributes>\n';
    	XML = XML + '              <Attribute>\n';
    	XML = XML + '                <AttributeID>1</AttributeID>\n';
    	XML = XML + '                <Name>Extended Warranty</Name>\n';
    	XML = XML + '                <Value>One Year Warranty</Value>\n';
    	XML = XML + '                <Price>10.00</Price>\n';
    	XML = XML + '              </Attribute>\n';
    	XML = XML + '            </Attributes>\n';
    	XML = XML + '          </Item>\n';
    	XML = XML + '        </Items>\n';
    	XML = XML + '        <Totals>\n';
    	XML = XML + '          <Total id="0" name="subtotal" class="subtotal" impact="none">240.00</Total>\n';
    	XML = XML + '          <Total id="0" name="shipping" class="shipping" impact="add">0.00</Total>\n';
    	XML = XML + '          <Total id="0" name="sales tax" class="tax" impact="add">14.40</Total>\n';
    	XML = XML + '          <Total id="0" name="coupon" class="coupon" impact="subtract">10.00</Total>\n';
    	XML = XML + '        </Totals>\n';
    	XML = XML + '      </Order>\n';

    	XML = XML + '    </Orders>\n';
    	XML = XML + '  </Salesforce>\n';
        // Create a fake response.
        HttpResponse res = new HttpResponse();
        res.setHeader('Content-Type', 'text/xml;charset=utf-8');
        res.setBody(XML);
        res.setStatusCode(200);
        
        // return response.
        return res;
    }
}